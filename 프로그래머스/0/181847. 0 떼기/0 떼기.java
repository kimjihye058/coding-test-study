class Solution {
    public String solution(String n_str) {
        String answer = "";
        // 정수로 바뀐 값을 담기 위해 num 변수 선언
        int num = Integer.parseInt(n_str);  
        // 다시 answer에 문자열로 바꿔서 담기
        return answer = String.valueOf(num);
    }
}